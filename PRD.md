# PRD — 오늘 뭐 먹지? (DWNC 10기 2차 코딩데이 · AI 메뉴 추천)

## 1. 한 줄 정의
점심·저녁 메뉴 고르기 귀찮은 사람이 버튼 몇 개 누르면 지금 날씨까지 반영한 메뉴 3개를 이유와 함께 받는 웹 서비스.

## 2. 누가 쓰는가
12시에 "뭐 먹지" 단톡이 30분째 답 없는 대학생. 폰으로 열어서 30초 안에 후보 3개 받고 톡방에 던진다.

## 3. 사용 순서
1. 접속 → 지역·기분·예산·동행·못 먹는 음식 버튼으로 선택 (타이핑 없음)
2. 날씨는 기상청 API로 자동 표시 (선택한 지역 기준)
3. "추천받기" → 로딩 → 메뉴 카드 3개 (메뉴명, 한 줄 이유, 어울리는 사이드/음료)
4. 마음에 안 들면 "다시 추천"

## 4. 만들 기능
- 조건 입력 폼 (선택형 버튼)
- 기상청 초단기실황 API로 현재 기온·강수 자동 조회
- Gemini API 호출 → JSON 강제 → 카드 3개 렌더

## 5. 화면 (2개)
① 입력 폼 ② 결과 카드. 한 페이지 안에서 전환. 밝은 미니멀, 포인트 컬러 오렌지, 모바일 우선.

## 6. 저장할 내용
없음 (Non-goal). 서버 상태 없음, 요청마다 API 호출.

## 7. 이상한 상황에서는
- 기상청 API 실패 → 날씨 "확인 불가"로 표시하고 추천은 진행
- Gemini 응답 JSON 파싱 실패 → 백틱 제거 후 재시도, 그래도 실패면 "다시 시도" 안내
- 키 누락 → 서버에서 500 + 명확한 메시지

## 8. 안 만들 것
로그인, 추천 기록 저장, 식당 검색/지도, 리뷰, 공유 이미지 생성.

## 기술 스택
Next.js 16 (App Router) + TypeScript + Tailwind v4 + Gemini API (@google/genai) + 기상청 단기예보 API + Vercel
